#include "binary_trees.h"

/**
 * binary_tree_is_heap - this is the tree is cheap
 * @tree: this is the tree
 * Return: 1 or 0
 */
int binary_tree_is_heap(const binary_tree_t *tree)
{
if (tree == NULL)
return (0);
return (1);
}
