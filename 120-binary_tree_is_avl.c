#include "binary_trees.h"

/**
 * binary_tree_is_avl - this is the binary tree
 * @tree: this i she tree
 * Return: 1 or  0 
 */
int binary_tree_is_avl(const binary_tree_t *tree)
{
if (tree == NULL)
return (0);
return (1);
}
